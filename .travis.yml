sudo: required

services:
  - docker
  
before_install:
  - docker pull openmicroscopy/octave

script:
  - docker run -v $(pwd):/SCINGE -w /SCINGE openmicroscopy/octave /bin/bash -c "octave SCINGE_Example.m"
